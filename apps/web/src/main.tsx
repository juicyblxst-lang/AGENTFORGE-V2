import React, { useEffect, useMemo, useState } from 'react';
import { createPublicClient, createWalletClient, custom, http, parseEventLogs, formatUnits } from 'viem';
import { bscTestnet } from 'viem/chains';
import { createRoot } from 'react-dom/client';
import './styles.css';

type Agent = { key: string; agentId: string; agentRegistry: string; name: string; description: string; owner?: string; agentWallet?: string; identityVerified?: boolean; categories: string[]; skills: any[]; endpoints: { type: string; url: string }[]; reputation: any };
type RuntimeConfig = { chainId: number; identityRegistry: string; commerce: string; router: string; policy: string; rpc: string; network: string };
type JobRecord = { status: string; result?: any; chain?: { statusName: string; budget: number; submitted_at: number; expired_at: number; deliverable: string; provider: string; client: string }; error?: string; create_tx?: string; register_tx?: string; budget_tx?: string; approval_tx?: string; fund_tx?: string; submit_tx?: string; settle_tx?: string };

const API = import.meta.env.VITE_API_URL ?? 'https://agentforge-v2-api.onrender.com';
const REGISTRY = 'eip155:97:0x8004A818BFB912233c491871b3d84c89A494BD9e';
const STATIC_AGENTS: Agent[] = [
  {key:`${REGISTRY}:2188`,agentId:'2188',agentRegistry:REGISTRY,name:'AgentForge Rebalancing Agent',description:'ERC-8004 agent for automated portfolio rebalancing on BNB Chain.',owner:'0x0000000000000000000000000000000000000001',agentWallet:'0x0000000000000000000000000000000000000001',identityVerified:true,categories:['rebalancing'],skills:['rebalance','liquidity','range'],endpoints:[{type:'http',url:`${API}/agent/rebalancing`}],reputation:null},
  {key:`${REGISTRY}:2189`,agentId:'2189',agentRegistry:REGISTRY,name:'AgentForge Grid Trading Agent',description:'ERC-8004 agent for grid trading strategies on BNB Chain.',owner:'0x0000000000000000000000000000000000000001',agentWallet:'0x0000000000000000000000000000000000000001',identityVerified:true,categories:['grid-trading'],skills:['grid','dca','bot'],endpoints:[{type:'http',url:`${API}/agent/grid-trading`}],reputation:null},
  {key:`${REGISTRY}:2190`,agentId:'2190',agentRegistry:REGISTRY,name:'AgentForge Yield Optimisation Agent',description:'ERC-8004 agent for yield optimisation on BNB Chain.',owner:'0x0000000000000000000000000000000000000001',agentWallet:'0x0000000000000000000000000000000000000001',identityVerified:true,categories:['yield-optimisation'],skills:['yield','apr','liquidity'],endpoints:[{type:'http',url:`${API}/agent/yield-optimisation`}],reputation:null},
  {key:`${REGISTRY}:2191`,agentId:'2191',agentRegistry:REGISTRY,name:'AgentForge Health Factor Monitoring Agent',description:'ERC-8004 agent for health factor monitoring on BNB Chain.',owner:'0x0000000000000000000000000000000000000001',agentWallet:'0x0000000000000000000000000000000000000001',identityVerified:true,categories:['health-factor'],skills:['liquidation','health','risk'],endpoints:[{type:'http',url:`${API}/agent/health-factor-monitoring`}],reputation:null}
];
const FALLBACK_CFG:RuntimeConfig={chainId:97,identityRegistry:'0x8004A818BFB912233c491871b3d84c89A494BD9e',commerce:'0xa206c0517b6371c6638cd9e4a42cc9f02a33b0de',router:'0xd7d36d66d2f1b608a0f943f722d27e3744f66f25',policy:'0x4f4678d4439fec812ac7674bb3efb4c8f5fb78a6',rpc:'https://data-seed-prebsc-1-s1.bnbchain.org:8545',network:'bsc-testnet'};
const commerceAbi=[
 {type:'function',name:'createJob',stateMutability:'nonpayable',inputs:[{name:'provider',type:'address'},{name:'evaluator',type:'address'},{name:'expiredAt',type:'uint256'},{name:'description',type:'string'},{name:'hook',type:'address'}],outputs:[{type:'uint256'}]},
 {type:'function',name:'fund',stateMutability:'nonpayable',inputs:[{name:'jobId',type:'uint256'},{name:'expectedBudget',type:'uint256'},{name:'optParams',type:'bytes'}],outputs:[]},
 {type:'function',name:'getJob',stateMutability:'view',inputs:[{name:'jobId',type:'uint256'}],outputs:[{type:'tuple',components:[{name:'id',type:'uint256'},{name:'client',type:'address'},{name:'provider',type:'address'},{name:'evaluator',type:'address'},{name:'description',type:'string'},{name:'budget',type:'uint256'},{name:'expiredAt',type:'uint256'},{name:'status',type:'uint8'},{name:'hook',type:'address'},{name:'submittedAt',type:'uint256'},{name:'deliverable',type:'bytes32'}]}]},
 {type:'function',name:'paymentToken',stateMutability:'view',inputs:[],outputs:[{type:'address'}]},
 {type:'event',name:'JobCreated',inputs:[{indexed:true,name:'jobId',type:'uint256'},{indexed:true,name:'client',type:'address'},{indexed:true,name:'provider',type:'address'},{indexed:false,name:'evaluator',type:'address'},{indexed:false,name:'expiredAt',type:'uint256'},{indexed:false,name:'hook',type:'address'}]}
] as const;
const routerAbi=[{type:'function',name:'registerJob',stateMutability:'nonpayable',inputs:[{name:'jobId',type:'uint256'},{name:'policy',type:'address'}],outputs:[]}] as const;
const erc20Abi=[{type:'function',name:'approve',stateMutability:'nonpayable',inputs:[{name:'spender',type:'address'},{name:'amount',type:'uint256'}],outputs:[{type:'bool'}]},{type:'function',name:'decimals',stateMutability:'view',inputs:[],outputs:[{type:'uint8'}]}] as const;
const policyAbi=[{type:'function',name:'disputeWindow',stateMutability:'view',inputs:[],outputs:[{type:'uint256'}]}] as const;

async function fetchWithTimeout(input: RequestInfo | URL, init: RequestInit = {}, timeoutMs = 15000): Promise<Response> {
 const controller = new AbortController();
 const timer = window.setTimeout(() => controller.abort(), timeoutMs);
 try { return await fetch(input, {...init, signal: controller.signal}); }
 finally { window.clearTimeout(timer); }
}

function App(){
 const[cfg,setCfg]=useState(FALLBACK_CFG);const[agents,setAgents]=useState<Agent[]>(STATIC_AGENTS);const[cat,setCat]=useState('all');const[query,setQuery]=useState('');const[account,setAccount]=useState<`0x${string}`>();const[selected,setSelected]=useState<Agent>();const[busy,setBusy]=useState('');const[jobId,setJobId]=useState('');const[record,setRecord]=useState<JobRecord>();const[error,setError]=useState('');
 const pub=useMemo(()=>createPublicClient({chain:bscTestnet,transport:http(cfg.rpc)}),[cfg.rpc]);
 useEffect(()=>{let cancelled=false;const load=async()=>{for(let i=0;i<10;i++){try{const[cfgRes,agentsRes]=await Promise.all([fetchWithTimeout(`${API}/api/config`,{},10000),fetchWithTimeout(`${API}/api/agents?limit=100`,{},15000)]);if(!cfgRes.ok||cancelled)return;setCfg(await cfgRes.json());const ag=await agentsRes.json();if(ag.agents?.length)setAgents(ag.agents);return}catch{if(cancelled)return;if(i<9)await new Promise(r=>setTimeout(r,4000));}}};load();return()=>{cancelled=true}},[]);
 useEffect(()=>{if(!jobId)return;let active=true;const f=async()=>{try{const r=await fetchWithTimeout(`${API}/api/jobs/${jobId}`,{},10000);if(r.ok&&active)setRecord(await r.json())}catch{}};f();const i=setInterval(f,5000);return()=>{active=false;clearInterval(i)}},[jobId]);
 useEffect(()=>{const eth=(window as any).ethereum;if(!eth)return;const syncAccounts=async()=>{try{const chainHex=await eth.request({method:'eth_chainId'});const chainId=Number.parseInt(chainHex,16);const accounts=await eth.request({method:'eth_accounts'});if(chainId!==cfg.chainId){setAccount(undefined);setSelected(undefined);setError(`Wrong network. Switch wallet to BSC Testnet (chain ${cfg.chainId}).`);return}setAccount(accounts?.[0]?accounts[0] as `0x${string}`:undefined)}catch{setAccount(undefined)}};const onAccounts=()=>syncAccounts();const onChain=()=>syncAccounts();eth.on?.('accountsChanged',onAccounts);eth.on?.('chainChanged',onChain);syncAccounts();return()=>{eth.removeListener?.('accountsChanged',onAccounts);eth.removeListener?.('chainChanged',onChain)}},[cfg.chainId]);
 const shown=agents.filter(a=>(cat==='all'||a.categories.includes(cat))&&(!query||`${a.name} ${a.description} ${a.skills.join(' ')} ${a.categories.join(' ')}`.toLowerCase().includes(query.toLowerCase())));
 async function connect(){try{setError('');const eth=(window as any).ethereum;if(!eth)throw Error('No browser wallet detected. Install MetaMask or another EVM wallet.');const w=createWalletClient({chain:bscTestnet,transport:custom(eth)});const[a]=await w.requestAddresses();const chain=await w.getChainId();if(chain!==cfg.chainId){try{await w.switchChain({id:cfg.chainId})}catch{throw Error(`Switch wallet to BSC Testnet (chain ${cfg.chainId}).`)}}setAccount(a)}catch(e:any){setError(e.shortMessage||e.message||String(e))}}
 function disconnect(){setAccount(undefined);setSelected(undefined);setJobId('');setRecord(undefined);setBusy('');setError('')}
 async function persist(id:string,fields:any){await fetchWithTimeout(`${API}/api/jobs/${id}/record`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(fields)},10000)}
 async function hire(agent:Agent){if(!account||busy!=='')return;setSelected(agent);setError('');setBusy('Preparing hire…');try{let prepR:Response;try{prepR=await fetchWithTimeout(`${API}/api/hire/prepare`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({agent_id:agent.agentId,agent_registry:agent.agentRegistry,client:account,description:`AgentForge task: execute ${agent.name}`})},15000)}catch(e:any){if(e?.name==='AbortError')throw Error('Agent service did not respond within 15 seconds. Please try again.');throw e}let prep:any;try{prep=await prepR.json()}catch{throw Error(`Agent preparation returned an invalid response (${prepR.status}).`)}if(!prepR.ok)throw Error(prep.detail||'Agent validation failed');const provider=prep.provider as `0x${string}`;const w=createWalletClient({chain:bscTestnet,transport:custom((window as any).ethereum)});const dispute=await pub.readContract({address:cfg.policy as `0x${string}`,abi:policyAbi,functionName:'disputeWindow'});const expiry=BigInt(Math.floor(Date.now()/1000))+BigInt(dispute)+600n;setBusy('Creating ERC-8183 job…');const tx=await w.writeContract({account,address:cfg.commerce as `0x${string}`,abi:commerceAbi,functionName:'createJob',args:[provider,cfg.router as `0x${string}`,expiry,`AgentForge:${agent.agentRegistry}:${agent.agentId}`,cfg.router as `0x${string}`]});const rc=await pub.waitForTransactionReceipt({hash:tx});if(rc.status!=='success')throw Error('createJob reverted');const logs=parseEventLogs({abi:commerceAbi,eventName:'JobCreated',logs:rc.logs});const id=logs[0]?.args.jobId;if(id===undefined)throw Error('JobCreated event not found');setJobId(id.toString());await persist(id.toString(),{agent_id:agent.agentId,agent_registry:agent.agentRegistry,client:account,create_tx:tx});setBusy('Registering job…');const rtx=await w.writeContract({account,address:cfg.router as `0x${string}`,abi:routerAbi,functionName:'registerJob',args:[id,cfg.policy as `0x${string}`]});const rrc=await pub.waitForTransactionReceipt({hash:rtx});if(rrc.status!=='success')throw Error('registerJob reverted');await persist(id.toString(),{register_tx:rtx});setBusy('Provider setting budget…');const br=await fetchWithTimeout(`${API}/api/jobs/${id}/budget`,{method:'POST'},30000);let bj:any;try{bj=await br.json()}catch{throw Error(`Budget service returned an invalid response (${br.status}).`)}if(!br.ok)throw Error(bj.detail||'Budget failed');const chainBudget=bj.onChain?.budget;if(chainBudget===undefined||chainBudget===null)throw Error('Budget transaction succeeded but on-chain budget was not returned');const amount=BigInt(chainBudget);if(amount<=0n)throw Error('Provider returned zero budget');const token=await pub.readContract({address:cfg.commerce as `0x${string}`,abi:commerceAbi,functionName:'paymentToken'});setBusy('Approving token…');const atx=await w.writeContract({account,address:token as `0x${string}`,abi:erc20Abi,functionName:'approve',args:[cfg.commerce as `0x${string}`,amount]});const arc=await pub.waitForTransactionReceipt({hash:atx});if(arc.status!=='success')throw Error('Approval reverted');await persist(id.toString(),{approval_tx:atx});setBusy('Funding escrow…');const ftx=await w.writeContract({account,address:cfg.commerce as `0x${string}`,abi:commerceAbi,functionName:'fund',args:[id,amount,'0x']});const frc=await pub.waitForTransactionReceipt({hash:ftx});if(frc.status!=='success')throw Error('Fund reverted');await persist(id.toString(),{fund_tx:ftx});setBusy('Hire funded — agent executing…')}catch(e:any){setError(e.shortMessage||e.message||String(e));setBusy('')}}
 const status=record?.chain?.statusName||record?.status||'not started';
 return <div className="shell"><header><div className="brand"><span>AGENT</span>FORGE</div><div className="network">BSC TESTNET <i/></div><button onClick={account?disconnect:connect} className="wallet">{account?account.slice(0,6)+'…'+account.slice(-4):'Connect wallet'}</button></header><main><section className="hero"><div><p className="eyebrow">THE BNB AGENT MARKETPLACE</p><h1>Hire intelligence.<br/><em>On-chain.</em></h1><p className="sub">Discover verified ERC-8004 agents on BNB Chain and activate them through real ERC-8183 escrow.</p></div><div className="orb"><div className="ring r1"/><div className="ring r2"/><div className="core">AF</div></div></section><div className="toolbar"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search agents, skills, capabilities…"/></div><nav className="cats">{[['all','All agents'],['rebalancing','Rebalancing'],['grid-trading','Grid trading'],['yield-optimisation','Yield optimisation'],['health-factor','Health factor']].map(([k,v])=><button className={cat===k?'active':''} onClick={()=>setCat(k)} key={k}>{v}</button>)}</nav><div className="bar"><span>{shown.length} verified</span><span>ERC-8004 · CHAIN 97</span></div>{error&&<div className="error">{error}</div>}<section className="grid">{shown.map(a=><article className={`card ${selected?.agentId===a.agentId?'selected':''}`} key={a.key} onClick={()=>setSelected(a)}><div className="card-top"><span className="tag">ERC-8004</span><span className="dot">●</span></div><h2>{a.name}</h2><p>{a.description}</p><div className="skills">{a.skills.slice(0,4).map((s:any)=><span key={String(s)}>{String(s)}</span>)}</div><button className="hire" disabled={!account||busy!==''} onClick={e=>{e.stopPropagation();hire(a)}}>{selected?.agentId===a.agentId&&busy?busy:'Hire agent'}</button></article>)}</section>{(jobId||busy||record)&&<section className="status"><div><strong>Job {jobId||'—'}</strong><span>{busy||status}</span></div>{record?.chain&&<small>Budget: {formatUnits(BigInt(record.chain.budget||0),0)} · Provider: {record.chain.provider}</small>}{record?.result&&<pre>{JSON.stringify(record.result,null,2)}</pre>}{record?.error&&<div className="error">{record.error}</div>}</section>}</main></div>}

createRoot(document.getElementById('root')!).render(<App/>);
